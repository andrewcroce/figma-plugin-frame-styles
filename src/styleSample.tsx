export const StyleSample = ({styles}) => {
  const {
    fills,
    strokes,
    topLeftRadius,
    topRightRadius,
    bottomLeftRadius,
    bottomRightRadius,
    effects,
    blendMode
  } = styles

  const css = {

  }
  return (
    <div></div>
  )
}
